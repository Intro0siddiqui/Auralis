# Takeover — Auralis v2.5.18

## Objective
- Ship visible `Download/Auralis/` dual-save (internal kept for `Range`) via `MediaStore` API29+ and replace `test-android-e2e` YouTube download with player-working `sdcard copy → scan → play` (`WARN-only` MediaStore); fix player/UI regressions (`No Track Selected` lag, dead `Next`, swallowed mobile taps, `viewTransition` superimposed views, spontaneous `Welcome` jumps, `Download→Home` redirect at `00:40.5`, audio select no-op) and keep CI `Build & Release` green with releases `v2.5.11 → v2.5.18`. v2.5.18 specifically fixes `Download→Home` `00:40.5` via `download.html onsubmit="return false"` + `downloads.js` rebind every `htmx` swap + `views.js` sync guard before `await` + `core.js` `htmx:restored/pageshow/visibilitychange` + delegated `submit` fallback.

## Important Details
- Device/host: `2410CRP4CI` Xiaomi/uke_in/uke `16` `BP2A.250605.031.A3` HyperOS `16KB` `libauralis_lib.so p_align 0x4000` `zipalign -P 16` `WebView 150.0.7871.181`; Xiaomi Pad 7 tablet sidebar layout Android 16; Realme `RMX3785` 11x 5G `15` `SDK 35` `ColorOS 15` `WebView 150.0.7871.183` (wireless debugging `10.174.149.156:33995`/`41655` `guid adb-DA59V4KNJRAIHIBI-zq9ypK` vs Xiaomi `10.151.19.135:35673`/`44625` `085299`); host **now Codespace** `Linux 6.8.0-1052-azure x86_64` `codespace/codespace uid=1000` vs old Void `aarch64` `Termux proot root uid=0` `adb 35.0.2` `PROOT_L2S_DIR` net ns isolation (`/proc/net/tcp Permission denied`); `cargo 1.89.0` installed via `rustup` `2026-08-28`, `cargo check --lib 61s` + `cargo test 27/27` + `clippy -D warnings` + `fmt` + `node --test 27/27` all pass (no `__stack_chk_guard` blocker); `target 83G` deleted; Codespace migration done.
- Conventions/CSP: no `unwrap()` prod; `rodio 0.22.2` `features [playback,mp3,mp4,flac,vorbis,wav]` no `opus`; `jni 0.21.1` `INITIAL_VM` `ndk_context`; `tauri.conf.json:33` CSP `default-src 'self' tauri: data: blob: ipc: http://ipc.localhost; img-src 'self' data: blob: asset: https://i.ytimg.com https://*.ytimg.com; media-src 'self' data: blob: asset: ipc: http://ipc.localhost; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; connect-src 'self' ipc: http://ipc.localhost https://*.googlevideo.com https://*.ytimg.com https://i.ytimg.com https://www.youtube.com https://youtubei.googleapis.com https://*.youtube.com https://jnn-pa.googleapis.com https://www.google.com https://*.google.com; font-src 'self' data: https:`; all `ui/vendor/` vendored.
- Resolver/PO: `ui/js/youtube.js:258` `ui/vendor/youtubei.esm.mjs 689KB 18.0.0`; `nativeFetch→http_fetch` `reqwest`; `extFromMime video/mp4→mp4 else m4a/webm/opus` `orderedClients` `poToken?[IOS,ANDROID,ANDROID_VR,TV,MWEB,WEB]:[TV,ANDROID_VR,MWEB,WEB,IOS,ANDROID]` `uaMap Referer https://www.youtube.com/ Origin`; `bgutils-js@4.0.3` `po_token.js:86` `jnn-pa.googleapis.com AIzaSyDyT5W...` `vendor:83 sabr!=1&& removed → this.po_token&&a.searchParams.set('pot',…)` + `youtube.js:705 defensive &pot=`; `downloads.js:7 _handle403AutoRetry TV→ANDROID+pot→WEB_SAFARI once` `downloads.rs:54 DIAGNOSTIC` `core.js:72 is403`.
- CI `.github/workflows/build.yml:1` `TAURI_CLI 2.11.4` `NDK 27.2.12479018` `compileSdk/targetSdk 36`: `lint` (`cargo fmt --check`+`clippy -D warnings`+`audit`) `build-linux` (`cargo build --release`+`cargo test --all-features`+`node --test youtube_resolver.test.js 27`+`xvfb desktop_real_e2e.js + desktop_download_player_e2e.js`) `build-macos`/`build-windows` (tag only) `build-android --split-per-abi aarch64+x86_64 zipalign -P 16 apksigner llvm-readelf 0x4000` (tag only) `test-android-e2e pixel_6 api33 x86_64 reactivecircus/android-emulator-runner@v2 run_emulator_test.sh→e2e_player_test.js CDP 9222` `ci needs [lint,build-linux,build-android,test-android-e2e]` `release needs [build-linux,build-macos,build-windows,build-android,lint]`; triggers `push main/tags v* + PR main + workflow_dispatch + schedule 0 3 * * *`; `concurrency cancel-in-progress`; `live-youtube-e2e` deleted `819eddb`; `v2.5.18 runs 33091120151 main success 9m + 33091155645 tag success 13m`.
- Download/MediaStore: `downloader.rs:91 ALLOWED_EXTS sanitize 192 join 198 UUID 8-char dedup 534`; `src/lib.rs:322 download_dir=app_data_dir/downloads`; `library.rs:142 AndroidScanner::scan_sandboxed_dir` scans `app_data_dir/music+downloads`; `android_downloads.rs publish_to_downloads mime_for_ext cached_vm OnceLock<JavaVM> cached_copy_for_path publish_q IS_PENDING 1 → insert MediaStore.Downloads.EXTERNAL_CONTENT_URI → openOutputStream 64KB → is_pending 0 → /storage/emulated/0/Download/Auralis/<name>`; `downloader.rs:515 #[cfg(android)] publish_to_downloads` gated by `Settings::load().use_system_downloads` dual-save; `player.rs:124 File::open fallback cached_copy_for_path`; `settings.rs:98 use_system_downloads default_true 119` `settings.html:90 toggle` `views.js:522+623 bind`; `verify_downloads_mediastore.sh WARN-only`.
- Player fixes: `player.rs:5 use Source source.total_duration() vs track.duration_secs >2s max`; `playback.rs:95 truncated_stop warn playback:error`; `player.js:364 fullArtist Unknown Artist`; `playback.rs:505 set_queue(track_ids,current_id) lib.rs:146` registered; `player.js:203 wireFullScreenElements` re-wire+`hydrateState` on `MutationObserver`/`htmx:afterSwap`/player-bar click; `player.js:508 next()/previous() async` library fallback `get_tracks limit 200 (idx+1)%len`; `modules/player.js:7 playTrack set_queue before play + hydrateState`; `modules/views.js:41 loadHomeView/library/albums/artists/playlists expected guard`; `renderTrackRows data-role=play-row/play-btn click+touchend touch-action:manipulation _syncPlayerBar hydrateState`.
- Screen recordings: `Screenrecorder-2026-08-25-17-19-50-565.mp4` `Vup1I1YpwiU 00:26 403 rr1---sn-gwpa-cived → 00:30 Download complete 1 added`; `Screenrecorder-2026-08-25-17-51-04-817.mp4` `v2.5.10 17s truncated stop`; Telegram `5_6087081488753369557.mp4` `00:35 f_0031 No Track Selected` `01:27 modal desync` `01:40 f_0160/01:42 f_0164/01:43 f_0166/01:44 f_0168` swallowed taps + `Next ⏭ Ok(None) Reached end of queue`; `Screenrecorder-2026-08-27-17-25-46-308.mp4` Xiaomi Pad 7 `00:08 header Albums/Library overlap` `00:13/00:17 spontaneous Welcome jump` `00:18 Download+Settings superimposed` `00:19 Albums blank` `00:25 SAF picker` `v2.5.15 Storage 23.71MB` `youtu.be/J60zDhfsWM`; `Screenrecorder-2026-08-27-20-52-06-985.mp4` `17MB 63s 00:40.5 Download→Home redirect reproduced` `Welcome` overwrites `page-downloads` after `Start Download` tap, no `Resolving source…` toast (root cause `downloads.js:164 dataset.bound` early return + `views.js:144` guard after `await` + `core.js:293` only `htmx:afterSwap`); `Screenrecorder-2026-08-27-21-49-47-431.mp4` `7.3MB 17s v2.5.18 21:49` `Welcome→File Manager→Library empty→Artists empty→Home→Recents→App info 23.33MB` — no `Download` tested, inconclusive for `00:40.5` fix, but `viewTransition` superimposition/header overlap absent (fixed); `Screenshot_2026-08-27-21-50-58-899` `Wireless debugging 10.151.19.135:35673` `pair 44625 085299` (Xiaomi) and `10.174.149.156:41655 016330` (Realme `RMX3785`).
- Releases/runs: `v2.5.11 3179afa` `v2.5.12 2167c66` `v2.5.13 b804820` `v2.5.15` `v2.5.16 f9c9673` `v2.5.17 345bd82` `v2.5.18 dbe3d09` `bf1d399` post-`v2.5.18` UI sync; GH `33084245531 main success 9m2s` `33084254372 v2.5.17 success 14m50s` `33091120151 main success 9m` `33091155645 v2.5.18 success 13m build-linux success build-android success E2E success macos 6m40s windows 14m26s lint 1m53s CI status success release success` `auralis-v2.5.18-android-arm64.apk 18.3MB` + `-x86_64 20.4MB`.
- User directives: `prefer JS tests`, `verify until ends`, `work independently`, `use sub agents in parallel (All files access)`, `ignore Jio 403`, `remove e2e youtube download → pure player sdcard copy`, `still doesn't upload WARN-only`, `monitor yourself until apk built`, `why e2e didn't find mistake`, `file name has download → e2e_download_test.js→e2e_player_test.js`, `run sub agenta/u can run in parallel/resume`, `update agents.md`, `use skills`, `u can use multiple agents in parallel`, `push to github codespace`, `update takeover.md`.

## Work State
### Completed
- Verifiers: `cargo check --target aarch64-linux-android` pass `clippy -D warnings` host+android pass `fmt --check` pass `node --check youtube.js/player.js/views.js/downloads.js` `node --test 27/27` `bash -n` pass; `rg 2.5.18 13 hits Cargo.toml:3 Cargo.lock:279 tauri.conf.json:4 package.json:3`.
- Clippy fix `android_downloads.rs:16 #[cfg(target_os="android")] use tracing::{info,warn}`; player subtitle `player.js:364`; 17s `player.rs duration reconcile` + `playback.rs truncated_stop warn`; MediaStore `cached_vm OnceLock<JavaVM>` + `use_system_downloads` gate; amended `3179afa feat(android): Download/Auralis MediaStore dual-save`.
- CI gaps analysis: `build-android skipped unless tags v*` (why `33067030191 build-android skipped`), `desktop_download_player_e2e.js tinyWavBytes single track execScript invoke('play') not touchend htmx:afterSwap htmx: void`, `schedule` removed intentionally.
- Pure player E2E `27eaa6d`: `desktop_download_player_e2e.js:165 Player seed import_audio_file e2e-test-seed.wav base64 scan→play`; `e2e_download_test.js:header Player-Working Test seedSdcardCopyHostSide ls /sdcard/Music/*.mp3 /sdcard/Download/*.mp3 cp/run-as/cat stat>10KB buildInPageTestExpression scan→get_tracks→play+DOM click→get_now_playing 12×700ms playback:progress run_emulator_test.sh [7/9] CDP_PORT node e2e_* verify_downloads_mediastore.sh WARN-only`.
- Telegram freeze fix `2167c66 v2.5.12`: `playback.rs:505 set_queue lib.rs register player.js hydrate+next/previous fallback modules/player.js set_queue before play modules/views.js delegate play-card/play-row click+touchend touch-action + _syncPlayerBar cargo check/clippy -D warnings node --check fmt pass bump 2.5.11→2.5.12`.
- E2E coverage `8a606c4`: desktop seed 2 files `a+b set_queue → Next advances Previous back modal/bar hydration barTitle 200ms guard delegated tap`; android `verifiedNext/verifiedPrev set_queue+player.next/previous delegated [data-role=play-row] click+touchend bar No track guard`.
- Rename `ebba691 R 100% e2e_download_test.js→e2e_player_test.js run_emulator_test.sh:131 if -f e2e_player_test.js then E2E_JS else desktop_download_player_e2e.js tryImport gated:true Origin header is not a valid URL node --check bash -n OK fix linux failure`.
- Release `b804820 chore(release) 2.5.12→2.5.13 tag v2.5.13 33068294653 success`.
- Xiaomi Pad 7 fix `f9c9673 v2.5.16`: `nav.html:9-44 + home.html:15,24 + views.js:199 hx-swap="innerHTML transition:true"→"innerHTML" grep transition:true 0`; `views.js loadLibraryView/home/albums/artists/playlists expected + DOM check bump 2.5.13→2.5.16 cargo check pass node --check views.js OK pushed main+tag v2.5.16 33071242614 success`.
- Precise guard fix `345bd82 v2.5.17`: `views.js:144 loadHomeView` replaced global `document.querySelector('.page-downloads,…')` + aggressive `!.page-home` abort with precise `#content`-scoped `if (_content && _content.querySelector('.page-downloads,.page-settings,.page-library,.page-albums,.page-artists,.page-playlists,.page-search,.page-sync')) return;` — allows initial empty `#content` to render home but aborts when `#content` already shows different page, fixing `Download→Home` redirect `00:13` + `audio select no-op`; `cargo check --all-targets success 24.45s node --check views/player/modules/player OK`; bump `2.5.16→2.5.17 cargo update tag -f v2.5.17 push origin main --follow-tags` → `33084245531 main success 9m2s` `33084254372 v2.5.17 success 14m50s`.
- AGENTS.md sync `v2.5.17` + `v2.5.18` preparation: Project Overview `v2.5.17 shipped`, `js/modules` delegation, `player.js next/previous fallback + wireFullScreenElements hydration`, `nav.html hx-swap innerHTML no transition + #content guard v2.5.16→v2.5.17 precise`, Phase 2/3 `set_queue` + `android_downloads publish_to_downloads WARN-only`, `version 2.5.17→2.5.18`, Android CI `v2.5.18` `auralis-v2.5.18-android-arm64.apk` + `e2e_player_test.js R 100%`, Downloads `v2.5.17→v2.5.18 dual-save since v2.5.11`, CI jobs table full triggers/when, Testing `desktop_download_player_e2e xvfb` + `run_emulator_test.sh CDP 9222 WARN-only`, Key Files expanded.
- Download→Home `00:40.5` fix `dbe3d09 v2.5.18`: `download.html:8,21 onsubmit="return false"`; `downloads.js:161 loadDownloadView` rebind every swap `if(form._auralisSubmitHandler) removeEventListener` → `_handler e.preventDefault()+stopPropagation` → `addEventListener` → `dataset.bound` after + same for `searchForm`; `views.js:144 sync guard before await _contentPre.querySelector(.page-downloads…) return + activeView!==expected` before `await get_tracks`; `core.js:293 bindHTMXEvents add htmx:restored + pageshow persisted + visibilitychange→loadDownloadView()` + `document submit` delegated `preventDefault` fallback `capture:true` (handles `htmx` cache/BF-cache + `Files` app return); `cargo check --lib success 13s` `clippy -D warnings success 35s` `node --check` `node --test 27/27` `fmt --check` pass; bump `2.5.17→2.5.18 cargo check --lib` updates `Cargo.lock:279` `tauri.conf.json:4` `package.json:3`; `git commit dbe3d09` `push origin main` `tag -f v2.5.18 push origin tag --force` → `33091120151 main success 9m (lint success build-linux success E2E success)` `33091155645 v2.5.18 success 13m (lint success build-linux success build-android success E2E success macos success windows success CI status success release success)` `auralis-v2.5.18-android-arm64.apk 18.3MB` released.
- Video analysis `2026-08-27-20-52-06-985.mp4` `63s` reproduces `00:40.5` `Download→Home` after `Start Download` (no `Resolving source…` toast) — hypothesis `dataset.bound` stale + `views.js` guard after `await` + `core.js` only `htmx:afterSwap`; `2026-08-27-21-49-47-431.mp4` `17s v2.5.18` shows `Welcome→File Manager→Library empty→Artists empty→App info 23.33MB` — no `Download` tested, `viewTransition` superimposition/header overlap absent (fixed), `Library empty` expected (needs `Scan Storage`), missing `empty-state` card low cosmetic.
- Post-`v2.5.18` UI sync `bf1d399 chore: sync remaining ui partials & views delegation`: `views.js` delegate `play-album/play-artist` via `data-role` + `touchend` + `touch-action:manipulation`, remove redundant `expected` guards in `loadLibraryView/albums/artists/playlists`, fix `Scan Storage` `label→button onclick triggerFolderScan`, improve `loadSearchView/loadSettingsView` rebind (removeListener before add, `dataset.bound` after, null guards for `downloads/sync/appearance`), `home.html/library.html/search.html/settings.html` button tweaks; carried over uncommitted tweaks before codespace.
- Codespace verification `2026-08-28` (`6.8.0-1052-azure x86_64` `codespace/codespace`): installed `rustup 1.89.0`, `cargo check --lib 61s` pass, `cargo fmt --check` pass, `cargo clippy --all-targets --all-features -- -D warnings` fixed `android.rs:213` + `desktop.rs:384` `meta.len() as u64 → meta.len()` (unnecessary_cast), now `clippy 21s` pass, `cargo test --all-features 27/27` pass (`3m51s`), `node --check views/downloads/core/youtube/player/bridge` pass, `node --test youtube_resolver.test.js 27/27` pass; no `__stack_chk_guard` proot blocker. Diff pending `2 files 2 lines` clippy fix.

### Active
- Device verification pending `v2.5.18` + `bf1d399` on `RMX3785` Realme 11x 5G `15` + Xiaomi Pad 7: after `adb pair` same Wi-Fi, `adb connect <CONNECT_PORT>` + `logcat -s chromium,WebViewConsole,Rust` while `Download Audio → paste https://youtu.be/DWUNnV_GOxU → Start Download` to confirm stays on `page-downloads` with `Resolving source…`/`Download started!` + `bf1d399` album/artist tap delegation; Codespace now can run `cargo build --release` + forward `adb` (no net ns isolation).
- Clippy fix pending commit: `android.rs:213` `desktop.rs:384` `meta.len() as u64 → meta.len()` — needs `git commit` + `push origin main` to keep `lint` green on next CI (would fail `clippy -D warnings`).
- **Autonomous testing infrastructure** (set up `2026-08-28 15:11` for "test it completely" directive): `Xvfb :99 1280x800x24 -ac` `fluxbox` `x11vnc :5900` `websockify :6080 → noVNC` + `python3 -m http.server 1420 --directory ui` `8080` all `setsid+disown` detached (services survive shell exit but **NOT** codespace session timeout — re-launch if `ss -tlnp` empty). All in `tmux` session `auralis`. **Verification gap noted** (`15:46`): `gh codespace ports visibility 6080:public -c $CODESPACE_NAME` returned `error getting tunnel port: 404 Not Found` + `gh codespace ports` returned `[]` — codespace port-forwarding infra appears inconsistent; user-side reachability was working for `1420` (frame extraction `record_2026-08-28-16-47-14` via device browser confirmed static `ui/` works + v2.5.18 `00:40.5` fix stays on `page-downloads`). **No autonomous test recording produced** — build attempt (`cargo build --release`) was killed when codespace session restarted; subsequent sessions cannot resume build due to `target/` wipe (was deleted at `83G`).
- **Frame-analyzed user recording** `record_2026-08-28-16-47-14_40deb401b9ffe8e1df2f1cc5ba480b12.mp4` `50.86s 480x1066 h264 60fps 11MB` (`tmpfiles.org/dl/wnwKBNdQh8ef`) — static `https://super-halibut-695w5j7pvq5g249vg-1420.app.github.dev/` reached on `Realme RMX3785`, **v2.5.18 `00:40.5` redirect bug confirmed FIXED** at `00:37-00:44` (paste YouTube URL → `Start Download` → stays on `page-downloads` → `No active downloads` because static preview has no Rust IPC). The other `v2.5.16/17` fixes (no `viewTransition` super-impose at `00:09`, no `Welcome` jumps, precise `#content` guard) all observable in frames. `Active Downloads` empty expected for static (needs `cargo tauri dev` or APK sideload).

### Blocked
- None — `v2.5.18` + `bf1d399` shipped, Codespace `cargo test`/`clippy` green; `proot` blockers cleared. **NEW**: codespace session resets kill `nohup`/`setsid` `cargo build` (`pid 88903` `12:04` build attempt was killed when session reset; `target/` wiped) — use `cargo build` foreground with timeout `~6m`, or push build to CI instead.

## Next Move
1. **For incoming agent resuming from this takeover**: First confirm `git status` shows `M android.rs M desktop.rs M takeover.md` (clippy fix + takeover sync still uncommitted); commit + push: `git add -u && git commit -m "fix(clippy): drop unnecessary u64 cast meta.len() as u64→meta.len()" && git push origin main`. Will trigger `lint` CI green (no version bump needed for pure clippy fix).
2. If user asks for autonomous testing again: verify services alive with `ss -tlnp | grep -E "1420|8080|5900|6080"`; if not, re-run the `setsid bash -c` launch sequence in "Autonomous testing infrastructure" above. **DO NOT** background `cargo build --release` across a session — codespace timeout kills it; run foreground with `timeout 900 cargo build --release` then launch `DISPLAY=:99 ./target/release/auralis`.
3. On-device smoke `v2.5.18` + `bf1d399` via wireless `adb`: `adb connect 10.174.149.156:<CONNECT_PORT>` → `adb -s <IP> logcat -v time -s chromium,WebViewConsole,Rust` → Realme `Download` paste `https://youtu.be/DWUNnV_GOxU` → `Start Download` → verify stays on `page-downloads`, `Resolving source…`, `download:progress` `download:completed` `scan_library_paths` `1 added` + `content query --uri content://media/external/downloads --where "_display_name like '%m4a%'"` `is_pending 0` `WARN-only` + album/artist card `data-role=play-album/play-artist` tap; Xiaomi Pad 7 check `Download` nav + `home` track tap + `Next`; capture `ScreenRecorder` if regression. APK sideload path (preferred over rebuilding): `gh release view v2.5.18 --json assets --jq '.assets[] | .browser_download_url'` → `auralis-v2.5.18-android-arm64.apk` `18.3MB` (Realme/Xiaomi) or `-x86_64.apk` `20.4MB` (emulator).
4. If regression: patch `ui/js/modules/views.js activeView/#content guard` or `modules/downloads.js _auralisSubmitHandler` or `core.js delegated submit`, `node --check && cargo check --all-targets`, bump `2.5.18→2.5.19` `cargo update`, `tag -f v2.5.19` and monitor `GH_REPO=Intro0siddiqui/Auralis gh run view <tag-run> --json jobs` until `build-linux success build-android success Android E2E success release success`.

## Relevant Files
- `Cargo.toml:3` `Cargo.lock:279` `package.json:3` `tauri.conf.json:4 33` `AGENTS.md:1` (updated `2.5.18`) `takeover.md` `PROJECT.md:4 §9` `upgrade_test.md:34` `README.md:178`
- `src/infrastructure/media/android_downloads.rs` `src/infrastructure/media/downloader.rs:86 111 192 306 401 515` `src/infrastructure/media/player.rs:5 124` `src/commands/playback.rs:505 set_queue` `src/commands/library.rs:142` `src/domain/models/settings.rs:98` `src/lib.rs:146 322`
- `ui/partials/nav.html:9-44 hx-swap` `ui/partials/home.html:15 24` `ui/partials/download.html:1 page-downloads onsubmit` `ui/index.html:61 hx-get player-full`
- `ui/js/modules/views.js:7 refreshCurrentView 41 loadLibraryView 144 loadHomeView 227 loadAlbumsView` `ui/js/modules/player.js:7 playTrack set_queue` `ui/js/player.js:203 wireFullScreenElements 508 next/previous` `ui/js/modules/downloads.js:7 161 loadDownloadView` `ui/js/modules/core.js:293 bindHTMXEvents` `ui/js/bridge.js` `ui/js/youtube.js:258` `ui/vendor/youtubei.esm.mjs` `ui/js/modules/po_token.js:86`
- `scripts/android/e2e_player_test.js` (ex `e2e_download_test.js:280 seedSdcardCopyHostSide`) `scripts/android/run_emulator_test.sh:131` `scripts/android/verify_downloads_mediastore.sh` `scripts/tests/desktop_download_player_e2e.js:165 Player seed 2-track queue` `scripts/tests/youtube_resolver.test.js`
- `.github/workflows/build.yml:21 164 462` `gen/android/app/build.gradle.kts compileSdk 36` `/sdcard/DCIM/ScreenRecorder/*.mp4` `Screenrecorder-2026-08-27-20-52-06-985.mp4` `Screenrecorder-2026-08-27-21-49-47-431.mp4` `5_6087081488753369557.mp4`

